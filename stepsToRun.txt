# === EVERY TIME YOU WANT TO START ===
POWERSHELL:

cd C:\Users\patel\Desktop\portfolio-mern
$env:ADMIN_SECRET="MySecret123"
npm run dev

# === IF SOMETHING BREAKS, FULL RESTART ===
Get-Process -Name node -ErrorAction SilentlyContinue | Stop-Process -Force
Start-Sleep -Seconds 2
cd C:\Users\patel\Desktop\portfolio-mern
$env:ADMIN_SECRET="MySecret123"
npm run dev

# === IF MONGODB IS NOT RUNNING ===
Start-Service MongoDB
# then start the server as usual

# === IF DEPENDENCIES ARE MISSING (rare) ===
cd C:\Users\patel\Desktop\portfolio-mern
npm run install-all
$env:ADMIN_SECRET="MySecret123"
npm run dev

# === TO STOP ===
# Press Ctrl+C in the dev terminal